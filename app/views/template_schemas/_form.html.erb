<%= form_with(model: template_schema, local: true, class: "rails-form") do |form| %>
  <% if template_schema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(template_schema.errors.count, "error") %> prohibited this template_schema from being saved:</h2>

      <ul>
      <% template_schema.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :template_schema_body %>
  </div>

  <div class="field">
    <%= form.label :ui_schema %>
    <%= form.text_area :ui_schema, id: :template_schema_ui_schema %>
  </div>


  <div class="actions">

    <%= link_to 'Back', template_schemas_path, class: "ui button" %>
    <%= form.submit %>

  </div>
<% end %>
