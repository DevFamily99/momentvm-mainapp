<%= form_with(model: page_activity, local: true, class: "rails-form") do |form| %>
  <% if page_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_activity.errors.count, "error") %> prohibited this page_activity from being saved:</h2>

      <ul>
      <% page_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :page_activity_user_id %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note, id: :page_activity_note %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :page_activity_type %>
  </div>

  <div class="field">
    <%= form.label :page_id %>
    <%= form.text_field :page_id, id: :page_activity_page_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
