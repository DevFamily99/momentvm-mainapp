<%= form_with(model: page_comment, local: true, class: "rails-form") do |form| %>
  <% if page_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_comment.errors.count, "error") %> prohibited this page_comment from being saved:</h2>

      <ul>
      <% page_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :page_id %>
    <%= form.text_field :page_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
